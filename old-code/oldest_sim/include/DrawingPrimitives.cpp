#include "DrawingPrimitives.h"

//generateSolidCircle(double center[2], unsigned int slices)
//{
//    double theta;
//
//        glVertex2f(x, y);
//        for (size_t j = 0; j < slices; j++) {
//            theta = 360.0f * j * PI_OVER_180 / slices;
//            glVertex2f(x + radius * cos(theta), y + radius * sin(theta));
//            theta = 360.0f * (j + 1) * PI_OVER_180 / slices;
//            glVertex2f(x + radius * cos(theta), y + radius * sin(theta));
//        }
//    glEnd();
//}
